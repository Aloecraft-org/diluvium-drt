[package]
name = "drt-connector-exec"
description = "The exec connector: host:exec/run, the honest escape hatch"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
async-trait = { workspace = true }
serde = { workspace = true }
rmpv = { workspace = true }
drt-caps = { workspace = true }
drt-connector = { workspace = true }
# The one thing std does not spell: a kill aimed at a process GROUP, so a
# deadline sweeps everything the child started and not just the child.
libc = "0.2"

[dev-dependencies]
pollster = "0.4"
tempfile = "3"
