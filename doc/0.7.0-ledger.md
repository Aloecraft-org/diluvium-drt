# 0.7.0 ledger

One line per step. Spec: `doc/Plan-0.7.0.md`. Resume from here if the
session dies.

| step | status | commit | acceptance |
|---|---|---|---|
| 1 ownership dimension (§2) | **done** | hash filled at step 2 | 1, 2, 5 (24 at the end) |
| 2 `crypto/derive` (§4) | in progress | — | 15, 18, 19, 20 (16 with 7) |
| 3 stream sockets + transfer (§3.1–3.2) | pending — CHECKPOINT A inside | — | 3, 6, 7, 14 |
| 4 vital handles (§3.3) | pending | — | 11 (hibernating owner), 23 |
| 5 readiness push (§3.4) | pending | — | 4, 13 (pins diluvium rev) |
| 6 datagram (§3.1) | **slipped** — CHECKPOINT B: owner said SLIP IT | — | 12 slips with it |
| 7 `kid` (§5) | pending | — | 17, 21 (16 with 2) |
| 8 park deadline (§6) | pending | — | 22 |
| 9 `Channel` generalisation (§8) | pending | — | — |

## Step notes

- **1.** `Caller::Root | Caller::Node(u32)` in `drt-connector`; that enum
  *is* §2.3's two-valued scope. `Handles<R>` keyed by `(Caller, HandleId)`,
  generic over the resource, labelled by kind — no socket, no fd. Trait
  gained `call_as` and `release`, both defaulted; `route`/`dispatch` keep
  their signatures (11 connector test files call them) and delegate as
  `Root`; `route_as`/`dispatch_as` carry the caller. `PumpHost::released`
  drains the dispatcher and keeps the attributed report in `take_lost()`;
  `detached` never touches it. **Deferred to step 3:** draining
  `take_lost()` to stderr in the drive loop — `drt-swarm` has no stderr, and
  nothing real can be lost until a socket exists. `async-trait` added as a
  dev-dependency of `drt-swarm` for the pump test only.

## Discrepancies (spec vs code, or spec vs spec)

- Acceptance 8, 9, 10 are plugin items; §7.0 defers plugins. Not built.
  Spec-internal inconsistency, recorded, not resolved here.

## TODO, not ours

- `doc/Messaging.md` §10.8 correction is upstream (diluvium). Not this
  repository's to make. Handles survive hibernation; §10.8 says they do
  not. File it upstream.

## Checkpoints

- A (step 3, before wiring any caller of transfer): write the transfer API
  to this ledger and stop.
- B (before step 6): SLIP IT. Skip unless told otherwise.
