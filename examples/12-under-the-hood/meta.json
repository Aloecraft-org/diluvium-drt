{
  "name": "12-under-the-hood",
  "teaches": "What every host.* call is underneath: the host/calls and host/replies queue pair, a token the host echoes back, and a reply of four fields — which is why a denial is an ordinary message rather than an exception.",
  "cmd": "drt run app.dlua",
  "needs_network": false,
  "normalise": [
    "s|^  time  *ok  *[0-9][0-9]*$|  time                   ok      <ms>|",
    "s|^  host.time()  *ok  *[0-9][0-9]*$|  host.time()            ok      <ms>|"
  ]
}
