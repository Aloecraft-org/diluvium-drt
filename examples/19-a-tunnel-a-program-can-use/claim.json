{
  "_comment": "The caller's half, from a file: a local port, one fresh leg to the relay per accepted connection. The URL is `claim` and `--local` is `bind`. `drt --config claim.json tunnel`.",

  "tunnel": {
    "claim": "ws://127.0.0.1:18490/s/fp?k=caller-key-for-the-example-only",
    "bind": "127.0.0.1:18492"
  }
}
