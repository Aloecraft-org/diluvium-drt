{
  "program": { "path": "app.dlua" },

  "_caps": "The ceiling. Nothing in this process, now or later, is outside it.",
  "caps": [
    { "capability": "host:fs/*" }
  ],

  "_connectors": "Off by default, all of them. A connector is wired to a PLACE — here, one directory — and this file never carries an application's filenames.",
  "connectors": {
    "fs": {
      "scope": {
        "scope": "./workspace",
        "access": "read",
        "max_bytes": 65536
      }
    }
  },

  "_omits": "example: omits budget, which a real deployment states; 06-budgets is the one that shows it."
}
