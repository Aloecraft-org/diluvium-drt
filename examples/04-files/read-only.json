{
  "_comment": "The other half of the app. This file and its neighbour differ in one word, and that word is `access`.",

  "program": { "path": "app.dlua" },

  "_caps": "The ceiling. `host:fs/*` covers all four verbs; it does not decide which of them the connector answers.",
  "caps": [
    { "capability": "host:fs/*" }
  ],

  "_connectors": "A connector is wired to a PLACE. The directory is a machine fact and belongs here; the filenames are the program's.",
  "connectors": {
    "fs": {
      "scope": {
        "scope": "./workspace",
        "access": "read",
        "max_bytes": 1024
      }
    }
  },

  "_omits": "example: omits budget, which a real deployment states; 06-budgets is the one that shows it."
}
